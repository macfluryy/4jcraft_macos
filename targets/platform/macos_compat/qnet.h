/* macOS stub: PS4 QNet socket library — not needed on macOS desktop builds.
 * All QNet-dependent code paths are guarded with #ifndef __linux__ (PS4-only).
 * This stub exists solely so that unguarded includes don't break compilation. */
#pragma once
